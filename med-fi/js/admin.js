$(document).ready(function() {

	// Semantic UI
	$('.menu .item').tab();
	$('.ui.checkbox').checkbox();
	$('.ui.selection.dropdown').dropdown();
	$('.special.cards .image').dimmer({
		on: 'hover'
	});

});
